
# Flags for both GCC and Clang
PKG_CXXFLAGS = -std=c++14 -O0 -s -g0


# Conditional section based on the compiler
ifdef GCC
  PKG_LIBS += -Wl,--gc-sections
else
  # For Clang, you might need `-dead_strip`
  PKG_LIBS += -Wl,-dead_strip 
endif
